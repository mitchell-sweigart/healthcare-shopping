<div class="d-flex flex-row align-items-center justify-content-around">
  <h1>Networks: <%= @health_plan_networks.count %></h1>

  <%= form_with url: import_health_plan_networks_path, class: "d-flex", method: :post do |form| %>
    <%= form.file_field :file, accept: ".csv", class: "form-control me-2" %>
    <%= form.button "Import", class: "btn btn-outline-success"  %>
  <% end %>

  <%= form_with url: bulk_delete_health_plan_networks_path, class: "d-flex", method: :post do |form| %>
    <%= form.file_field :file, accept: ".csv", class: "form-control me-2" %>
    <%= form.button "Delete", class: "btn btn-outline-danger" %>
  <% end %>

  <%= button_to "New Network", new_health_plan_network_path, class:"btn btn-primary", :method => :get  %>
</div>

<br/>
<br/>
<br/>

<form class="d-flex" role="search" action="/ratings">
  <input class="form-control me-2" name="q" type="search" placeholder="Search" aria-label="Search">
  <button class="btn btn-outline-success" type="submit">Search</button>
</form>

<br/>

<p style="color: green"><%= notice %></p>

<br/>
<br/>

<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col" id="codes-id">
                <span>ID</span>
            </th>
            <th scope="col" id="codes-code">
                <span>Name</span>
            </th>
            <th scope="col" id="codes-actions">
                <span></span>
            </th>
            <th scope="col" id="codes-actions">
                <span></span>
            </th>
            <th scope="col" id="codes-actions">
                <span></span>
            </th>
        </tr>
    </thead>

    <tbody>
        <% @health_plan_networks.each do |healthplannetwork| %>
            <tr class="align-middle">
                <th scope="row"><%= healthplannetwork.id %></th>
                <td><%= healthplannetwork.name %></td>
                <td>
                    <%= button_to "View", health_plan_network_path(healthplannetwork), class: "btn btn-outline-secondary", :method => :get  %>
                </td>
                <td>
                    <%= button_to "Edit", edit_health_plan_network_path(healthplannetwork), class: "btn btn-outline-secondary", :method => :get  %>
                </td>
                <td>
                    <%= button_to "Delete", health_plan_network_path(healthplannetwork), class: "btn btn-outline-danger", method: :delete %>
                </td>
            </tr>
        <% end %>
    </tobdy>
</table>